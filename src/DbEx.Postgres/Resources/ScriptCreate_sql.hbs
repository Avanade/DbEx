{{! Copyright (c) Avanade. Licensed under the MIT License. See https://github.com/Avanade/DbEx }}
{{! FILENAME:create-[lower (lookup Parameters 'Param1')]-[lower (lookup Parameters 'Param2')]-table }}
{{! PARAM:Param1=Schema }}
{{! PARAM:Param2=Name }}
-- Create table: "{{lookup Parameters 'Param1'}}"."{{lookup Parameters 'Param2'}}"

CREATE TABLE "{{lookup Parameters 'Param1'}}"."{{lookup Parameters 'Param2'}}" (
  "{{lookup Parameters 'Param1'}}_id" SERIAL PRIMARY KEY,
  -- "code" VARCHAR(50) NULL UNIQUE,
  -- "text" VARCHAR(250) NULL,
  -- "bool" BOOLEAN NULL,
  -- "date" DATE NULL,
  "created_by" VARCHAR(250) NULL,
  "created_date" TIMESTAMPTZ NULL,
  "updated_by" VARCHAR(250) NULL,
  "updated_date" TIMESTAMPTZ NULL
);